# AI Employee - Python Dependencies
# Silver Tier - All Watchers and MCP Servers

# Core dependencies (Bronze Tier)
watchdog>=4.0.0  # File system event monitoring

# Gmail Watcher (Silver Tier)
google-api-python-client>=2.100.0  # Gmail API
google-auth-httplib2>=0.1.0  # Gmail authentication
google-auth-oauthlib>=1.0.0  # OAuth for Gmail

# WhatsApp Watcher (Silver Tier)
playwright>=1.40.0  # Browser automation
# Note: playwright requires additional installation:
#   playwright install chromium

# LinkedIn Poster (Silver Tier)
# Uses playwright (already included)

# Task Scheduler (Silver Tier)
schedule>=1.2.0  # Job scheduling

# MCP Servers (Silver Tier)
# Email MCP uses google-api-python-client (already included)

# Development (optional)
# pytest>=7.4.0  # Testing
# black>=23.0.0  # Code formatting
# flake8>=6.0.0  # Linting
